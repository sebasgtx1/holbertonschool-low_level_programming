#include "main.h"
#include <stdio.h>

/**
 * print_buffer - prints a buffer (b)
 * @b: buffer.
 * @size: size of buffer.
 * Return: no return.
 */
void print_buffer(char *b, int size)
{
int s;

*b = 0;
size = 0;
s = 1;
*b = size + s;
}
