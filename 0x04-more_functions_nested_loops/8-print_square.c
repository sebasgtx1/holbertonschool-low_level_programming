#include "main.h"
/**
 * print_square -  draws an square
 * @size: size of the square to be printed
 * Return: no return
 */
void print_square(int size)
{
	int i, i2;

	for (i = 1; i <= size; i++)
	{
		for (i2 = 1; i2 <= size; i2++)
		{
		_putchar(35);

		}
	_putchar('\n');
}
if (size == 0)
{
_putchar('\n');
}
}
