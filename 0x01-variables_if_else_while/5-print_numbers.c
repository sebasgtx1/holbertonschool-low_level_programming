#include <stdio.h>

/**
 * main - prints a messge with printf
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int c;

for (c = 0; c < 10; ++c)
{
printf("%i", c);
}
putchar('\n');
return (0);
}
