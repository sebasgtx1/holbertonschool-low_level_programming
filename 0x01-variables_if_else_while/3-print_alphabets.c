#include <stdio.h>

/**
 * main - prints a messge with printf
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char c, d;

for (c = 'a'; c <= 'z'; ++c)
{
putchar(c);
}

for (d = 'A'; d <= 'Z'; ++c)
{
putchar(d);
}
return (0);
}
