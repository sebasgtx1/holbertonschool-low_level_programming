#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs -  checks for lowercase character
 * @a: is a number
 * Return: 0 (Success)
*/
int _abs(int a)
{
printf("%i", abs(a));
return (0);
}
